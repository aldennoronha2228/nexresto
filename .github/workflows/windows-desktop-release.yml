name: Windows Desktop Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: "Desktop app version (for example 1.0.1)"
        required: false
        type: string
      publish:
        description: "Publish release to GitHub"
        required: false
        default: true
        type: boolean
  push:
    tags:
      - "desktop-v*"

permissions:
  contents: write

jobs:
  build-windows-desktop:
    runs-on: windows-latest

    env:
      CSC_LINK: ${{ secrets.CSC_LINK }}
      CSC_KEY_PASSWORD: ${{ secrets.CSC_KEY_PASSWORD }}
      CSC_NAME: ${{ secrets.CSC_NAME }}
      WIN_CSC_LINK: ${{ secrets.WIN_CSC_LINK }}
      WIN_CSC_KEY_PASSWORD: ${{ secrets.WIN_CSC_KEY_PASSWORD }}
      WIN_CSC_NAME: ${{ secrets.WIN_CSC_NAME }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install desktop dependencies
        run: npm ci
        working-directory: desktop

      - name: Apply version from tag
        if: startsWith(github.ref, 'refs/tags/desktop-v')
        shell: pwsh
        run: |
          $version = "${{ github.ref_name }}".Replace('desktop-v', '')
          npm version $version --no-git-tag-version
        working-directory: desktop

      - name: Apply version from manual input
        if: github.event_name == 'workflow_dispatch' && github.event.inputs.version != ''
        run: npm version ${{ github.event.inputs.version }} --no-git-tag-version
        working-directory: desktop

      - name: Build installer
        run: npm run dist
        working-directory: desktop

      - name: Require code signing for publish
        if: startsWith(github.ref, 'refs/tags/desktop-v') || (github.event_name == 'workflow_dispatch' && github.event.inputs.publish == 'true')
        shell: pwsh
        run: |
          $hasGeneric = -not [string]::IsNullOrWhiteSpace("${{ secrets.CSC_LINK }}") -and -not [string]::IsNullOrWhiteSpace("${{ secrets.CSC_KEY_PASSWORD }}")
          $hasWindows = -not [string]::IsNullOrWhiteSpace("${{ secrets.WIN_CSC_LINK }}") -and -not [string]::IsNullOrWhiteSpace("${{ secrets.WIN_CSC_KEY_PASSWORD }}")
          if (-not ($hasGeneric -or $hasWindows)) {
            Write-Error "Code-signing is required for published installers. Set CSC_LINK + CSC_KEY_PASSWORD (or WIN_CSC_LINK + WIN_CSC_KEY_PASSWORD) in repository secrets."
            exit 1
          }

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: nexresto-windows-installer
          path: desktop/dist/*.exe

      - name: Verify installer signature
        if: startsWith(github.ref, 'refs/tags/desktop-v') || (github.event_name == 'workflow_dispatch' && github.event.inputs.publish == 'true')
        shell: pwsh
        working-directory: desktop
        run: |
          $expected = "${{ secrets.WIN_CSC_NAME }}"
          if ([string]::IsNullOrWhiteSpace($expected)) {
            $expected = "${{ secrets.CSC_NAME }}"
          }
          ./scripts/verify-windows-signature.ps1 -Path ./dist -ExpectedPublisher $expected

      - name: Publish GitHub release
        if: startsWith(github.ref, 'refs/tags/desktop-v') || (github.event_name == 'workflow_dispatch' && github.event.inputs.publish == 'true')
        run: npm run publish
        working-directory: desktop
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CSC_LINK: ${{ secrets.CSC_LINK }}
          CSC_KEY_PASSWORD: ${{ secrets.CSC_KEY_PASSWORD }}
          CSC_NAME: ${{ secrets.CSC_NAME }}
          WIN_CSC_LINK: ${{ secrets.WIN_CSC_LINK }}
          WIN_CSC_KEY_PASSWORD: ${{ secrets.WIN_CSC_KEY_PASSWORD }}
          WIN_CSC_NAME: ${{ secrets.WIN_CSC_NAME }}
