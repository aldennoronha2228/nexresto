# Database

Firestore is the primary production datastore for NexResto.

This folder documents schema and migration history for operational consistency.

- `schema.md` tracks major collections and key fields.
- `migrations/` stores manual migration scripts or notes.
